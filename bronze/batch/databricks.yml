# This is a Databricks asset bundle definition for batch.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: batch
  uuid: 9c82b034-2c29-4169-9935-af2da63a8d94

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-dfb14324-e95d.cloud.databricks.com
    variables:
      catalog: bronze
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-dfb14324-e95d.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/daniel.epr77@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/daniel.epr77@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: bronze
      schema: prod
    permissions:
      - user_name: daniel.epr77@gmail.com
        level: CAN_MANAGE
